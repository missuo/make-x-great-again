{
  "name": "make-x-great-again",
  "version": "0.0.1",
  "private": true,
  "description": "Make X Great Again (MXGA) — passive ambient X extension. Local classifier + curation store + shared Worker/D1.",
  "license": "AGPL-3.0-only",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "classify": "tsx src/cli.ts",
    "baseline:train": "tsx scripts/baseline/train.ts",
    "baseline:eval": "tsx scripts/baseline/eval.ts",
    "serve": "tsx src/server.ts",
    "release:zip": "bash scripts/release-zip.sh",
    "typecheck": "tsc --noEmit",
    "lint": "biome lint src test services/edge/src services/edge/app",
    "format": "biome format --write .",
    "test": "tsx --test test/**/*.test.ts"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/node": "^22.10.2",
    "tsx": "^4.23.0",
    "typescript": "^5.7.2"
  }
}
